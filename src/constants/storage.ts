/**
 * Supabase Storage bucket for place cover photos.
 * Used by place creation and place update photo uploads — keep a single name.
 */
export const PLACE_PHOTOS_BUCKET = 'place-photos';

/**
 * Supabase Storage bucket for profile avatars.
 * Used by Profile and Settings account photo uploads.
 */
export const PROFILE_AVATARS_BUCKET = 'profile-avatars';
