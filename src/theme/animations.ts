/**
 * @deprecated Import from `../theme/motion` or `../theme` (`motion`, `motionEasing`).
 * Kept for backward compatibility.
 */
export {
  duration,
  scale,
  spring,
  mapMotion,
  motion,
  motionEasing,
} from './motion';
